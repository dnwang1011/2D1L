{
  "name": "@2dots1line/shader-lib",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rm -rf dist src/generated && pnpm exec node ./scripts/bundle-shaders.js && tsc -b tsconfig.json --verbose --force",
    "lint": "eslint . --ext .ts",
    "test": "echo \"No tests for shader-lib yet\" && exit 0"
  },
  "dependencies": {
    "glob": "^10.3.10",
    "fs-extra": "^11.2.0"
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "typescript": "^5.3.3"
  }
} 