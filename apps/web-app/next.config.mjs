/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Add other Next.js configurations here if needed
  // Example for experimental features or webpack customization
  // experimental: {
  //   appDir: true, // Already true by default with latest Next.js
  // },
  // If you need to transpile packages from the monorepo:
  // transpilePackages: ['@2dots1line/shared-types', '@2dots1line/ui-components'],
};

export default nextConfig; 